# کاتالوگ جامع استفاده از Query Builder (استاندارد JSON:API)

این مستند تمامی حالت‌ها و ظرفیت‌های پیاده‌سازی شده در کلاس `JsonApiQueryOptions` و افزونه `JsonApiExtensions` را برای توسعه‌دهندگان فرانت‌اند (Frontend) لیست می‌کند.
با مطالعه‌ی این کاتالوگ، می‌توانید تمامی حالت‌های فیلتر کردن، مرتب‌سازی، دریافت روابط (Includes) و صفحه‌بندی را با ذکر مثال‌های دقیق در APIها فراخوانی کنید.

---

## 🗂 ۱. کاتالوگ فیلترها (Filtering)
سیستم فیلترینگ اکنون از تمام عملگرهای مقایسه‌ای و منطقی پیشرفته پشتیبانی می‌کند. برای استفاده از عملگرها، کافی است آن‌ها را در ابتدای مقدارِ فیلتر قرار دهید.

### ۱-۱. عملگرهای مقایسه‌ای (برابری، بزرگی و کوچکی)
- **برابر بودن (Exact Match):** اگر هیچ پیشوندی نگذارید (یا از `==` استفاده کنید)، دقیقاً همان مقدار جستجو می‌شود.
  - `?filter[age]=18` (سن برابر ۱۸ باشد)
- **نامساوی (`!=` یا `!`):**
  - `?filter[status]=!active` (وضعیت active نباشد)
  - `?filter[age]=!=18`
- **بزرگتر (`>`) و بزرگتر مساوی (`>=` یا `!<`):**
  - `?filter[price]=>1000` (قیمت بیشتر از هزار)
  - `?filter[price]=>=1000` (بیشتر یا مساوی هزار)
- **کوچکتر (`<`) و کوچکتر مساوی (`<=` یا `!>`):**
  - `?filter[age]=<30`
  - `?filter[age]=<=30`

### ۱-۲. فیلتر روی فیلدهای متنی (رشته‌ها)
اگر فیلد از نوع متن (String) باشد و از عملگرهای `==` یا `!=` استفاده **نکنید**، سیستم به صورت خودکار به صورت `Contains` (شامل کلمه بودن) جستجو می‌کند.
- `?filter[name]=ali` (نام *شامل* ali باشد)
- `?filter[name]==ali` (نام *دقیقاً* ali باشد)
- `?filter[name]=!ali` (نام *دقیقاً* ali نباشد)

### ۱-۳. فیلتر لیست مقادیر (IN و NOT IN)
برای جستجوی یک فیلد در میان چندین مقدار، مقادیر را با کاما جدا کنید.
- **عملگر `in:`**
  - `?filter[status]=in:1,2,3` (وضعیت برابر ۱، ۲ یا ۳ باشد)
- **عملگر `nin:` (Not In):**
  - `?filter[status]=nin:4,5` (وضعیت ۴ یا ۵ نباشد)

### ۱-۴. فیلترهای ترکیبی منطقی (AND و OR)
- **ترکیب با عملگر AND (پیش‌فرض):** تمام پارامترهای مجزایی که به عنوان فیلتر می‌فرستید، با `AND` ترکیب می‌شوند.
  - `?filter[is_active]=true&filter[age]=>20` (فعال باشد **و** سن بیشتر از ۲۰)
- **ترکیب با عملگر OR (روی یک فیلد):** با استفاده از علامت `|` (پایپ) می‌توانید چند شرط روی **یک فیلد** بگذارید.
  - `?filter[age]=>18|<10` (سن بیشتر از ۱۸ **یا** کمتر از ۱۰ باشد)
- **ترکیب با عملگر OR (روی فیلدهای مختلف):** با استفاده از کلید رزرو شده `$or` می‌توانید شروط فیلدهای مختلف را با یا ترکیب کنید. فرمت آن به شکل `field:value` با جداکننده `|` است.
  - `?filter[$or]=name:john|age:>18` (نام شامل john باشد **یا** سن بیشتر از ۱۸ باشد)

---

## 🔃 ۲. کاتالوگ مرتب‌سازی (Sorting)
مرتب‌سازی از طریق پارامتر `sort` انجام می‌شود. سیستم از تبدیل نام‌ها (مثل `created_at` به `CreatedAt`) پشتیبانی می‌کند.

### ۲-۱. مرتب‌سازی صعودی (Ascending) یک فیلد
- **حالت:** نمایش کاربران از کمترین سن به بیشترین.
- **کوئری:** `?sort=age`

### ۲-۲. مرتب‌سازی نزولی (Descending) یک فیلد
- **حالت:** نمایش آخرین مقالات (جدیدترین به قدیمی‌ترین).
- **کوئری:** `?sort=-created_at`

### ۲-۳. مرتب‌سازی چندگانه (Multi-column Sort)
زمانی که می‌خواهید اولویت اول با یک فیلد باشد و در صورت برابر بودن، اولویت دوم اعمال شود.
- **حالت:** مرتب‌سازی ابتدا بر اساس امتیاز (بیشترین به کمترین)، سپس در صورت برابریِ امتیاز، بر اساس نام (الفبا).
- **کوئری:** `?sort=-score,name`
- **رفتار سرور:** `ORDER BY Score DESC, Name ASC`

---

## 🔗 ۳. کاتالوگ دریافت روابط (Includes / Eager Loading)
برای دریافت دیتاهای وابسته (نویگیشن‌ها) که در جداول دیگر دیتابیس هستند، از پارامتر `include` استفاده می‌شود.

### ۳-۱. دریافت یک رابطه‌ی ساده
- **حالت:** دریافت کاربر به همراه مشخصات پروفایلش.
- **کوئری:** `?include=profile`

### ۳-۲. دریافت چند رابطه‌ی هم‌سطح
- **حالت:** دریافت یک مقاله، به همراهِ مشخصات نویسنده‌ی آن مقاله، و لیستِ برچسب‌های (Tags) مقاله.
- **کوئری:** `?include=author,tags`

### ۳-۳. دریافت روابط تو در تو (Nested Includes)
برای دسترسی به جدولی که خودش زیرمجموعه‌ی یک جدولِ وابسته است، از کاراکتر نقطه (`.`) استفاده می‌شود.
- **حالت:** دریافت یک پست، به همراه کامنت‌های آن پست، و علاوه بر آن واکشی اطلاعاتِ کاربری که هر کامنت را نوشته است.
- **کوئری:** `?include=comments.user`

### ۳-۴. دریافت روابط ترکیبی پیچیده
- **حالت:** دریافت یک سفارش فروشگاه، به همراه خریدار (Customer)، لیست آیتم‌های سفارش (Items)، و مشخصات محصولِ هر آیتم (Items.Product).
- **کوئری:** `?include=customer,items.product`

---

## 📄 ۴. کاتالوگ صفحه‌بندی (Pagination)
داده‌ها در صورت عدم ارسال پارامترها، به صورت پیش‌فرض در **صفحه اول** و با **تعداد ۱۵ آیتم** در هر صفحه (PageSize=15) برگشت داده می‌شوند.

### ۴-۱. رفتن به یک صفحه‌ی خاص (با تعداد پیش‌فرض)
- **حالت:** می‌خواهید دیتای صفحه سوم را بگیرید (با همان تعداد ۱۵ تایی دیفالت سرور).
- **کوئری:** `?page[number]=3`

### ۴-۲. تغییر تعداد نمایش در هر صفحه (Page Size)
- **حالت:** می‌خواهید دیتای صفحه اول را بگیرید اما می‌خواهید در هر صفحه ۵۰ رکورد وجود داشته باشد.
- **کوئری:** `?page[size]=50`

### ۴-۳. تغییر همزمان صفحه و تعداد (حالت معمول فرانت‌اند)
- **حالت:** کاربر روی دکمه "صفحه ۴" در یک جدولِ ۳۰ تایی کلیک کرده است.
- **کوئری:** `?page[number]=4&page[size]=30`

---

## 🎯 ۵. کاتالوگ انتخاب فیلدها (Select / Shaping)
توسط پارامتر `select` شما می‌توانید دقیقاً مشخص کنید سرور کدام فیلدها را در خروجی JSON برگرداند و کدام‌ها را حذف کند. این ویژگی برای کاهش حجم دیتای دریافتی (Payload) در سمت فرانت‌اند بسیار مفید است.

### ۵-۱. انتخاب فیلدهای خاص (Include Fields)
اگر می‌خواهید فقط چند فیلد خاص برگردانده شود، نام آن‌ها را با کاما جدا کنید. بقیه فیلدها از خروجی حذف خواهند شد.
- **حالت:** از کاربر فقط آیدی، نام و ایمیل را می‌خواهیم.
- **کوئری:** `?select=id,name,email`

### ۵-۲. حذف فیلدهای خاص (Exclude Fields)
اگر می‌خواهید همه فیلدها برگردند اما فقط چند فیلد خاص حذف شوند، قبل از نام آن‌ها علامت منفی (`-`) بگذارید.
- **حالت:** دریافت کاربر بدون پسورد و تاریخچه.
- **کوئری:** `?select=-password,-login_history`

### ۵-۳. ترکیب انتخاب و حذف
شما می‌توانید همزمان مشخص کنید چه فیلدی باشد و چه فیلدی نباشد (در این حالت ابتدا فیلدهای خواسته‌شده واکشی می‌شوند، سپس از بین آن‌ها، فیلدهای منفی حذف می‌گردند).
- **کوئری:** `?select=profile,-profile.age`

---

## 💡 نمونه‌ی نهایی پاسخ سرور (Response Structure)
زمانی که کوئری‌های بالا را می‌فرستید، دیتای دریافتی در قالب زیر به دست شما می‌رسد:

```json
{
  "success": true,
  "code": 200,
  "message": "عملیات با موفقیت انجام شد.",
  "data": {
    "data": [
      {
        "id": "...",
        // فیلدهای دیتا ...
      }
    ],
    "totalCount": 142 // کل رکوردهای دیتابیس که با شرایطِ فیلتر شما همخوانی دارند
  },
  "meta": null
}
```
*با محاسبه‌ی `Math.ceil(totalCount / page_size)` می‌توانید تعداد کل صفحات دکمه‌های Pagination را در فرانت‌اند رسم کنید.*
